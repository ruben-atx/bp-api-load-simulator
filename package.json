{
  "name": "bp-alert-api-load-simulator",
  "version": "1.0.0",
  "description": "This application will simulate requests made to the BigPanda Alert API. The volume of requests per time interval are configurable. Can be used to test throttling. ",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ruben Gutierrez",
  "license": "ISC",
  "dependencies": {
    "loadtest": "^3.0.8",
    "request": "^2.88.0",
    "request-promise": "^4.2.4",
    "yargs": "^14.0.0"
  }
}
